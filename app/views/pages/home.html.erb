<body class="bg-white">
  <div class="flex flex-col">
    <!-- トップバー -->
    <header class="flex items-center justify-between px-4 py-2 bg-white border-b">
      <div class="flex items-center">
        <span class="text-orange-600 text-xl font-bold">🍴 NonFryer recipes</span>
      </div>
      <div class="flex items-center space-x-2">
        <% if logged_in? %>
          <%= render "shared/logout" %>
        <% else %>
          <%= render "shared/login" %>
        <% end %>
        <%= link_to "＋", '#', class: "text-white bg-orange-600 rounded-full w-8 h-8 flex items-center justify-center" %>
      </div>
    </header>

    <!-- サイドバーとメインコンテンツのラップ -->
    <div class="flex relative">
      <!-- メインコンテンツ -->
      <main class="flex-1 ml-0 md:ml-64 transition-all duration-300">
        <!-- 検索バー -->
        <div class="flex items-center justify-between p-4">
          <%= render "shared/sidebar" %>
          <%= form_with url: '#', method: :get, local: true do |f| %>
            <%= f.text_field :search, placeholder: "レシピ検索", class: "flex-1 border border-gray-300 rounded px-2 py-1" %>
            <%= f.submit "検索", class: "bg-orange-600 text-white rounded px-4 py-1" %>
          <% end %>
        </div>
        <!-- レシピ表示部分 -->
      </main>
    </div>
  </div>
</body>
